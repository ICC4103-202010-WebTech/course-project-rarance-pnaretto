<%= form_with(model: organization_member, local: true) do |form| %>
  <% if organization_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_member.errors.count, "error") %> prohibited this organization_member from being saved:</h2>

      <ul>
        <% organization_member.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
