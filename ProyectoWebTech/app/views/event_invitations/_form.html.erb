
<head>
  <%= stylesheet_link_tag 'global', media:'all', 'data-turbolinks-track': 'reload' %>
</head>

<div class="columna-blanca" >

  <%= form_with(model: event_invitation, local: true) do |form| %>
    <%= form.hidden_field :event_id, value: $ev %>
    <div class="field">
      <%= form.label :eventUser %><br />
      <%= form.collection_select :user_id, User.order(:name), :id, :name, include_blank: true %>
    </div>
    <% if event_invitation.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(event_invitation.errors.count, "error") %> prohibited this event_invitation from being saved:</h2>

        <ul>
          <% event_invitation.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>


    <div class="actions">
      <%= form.submit("Invite") %>
    </div>
  <% end %>


</div>