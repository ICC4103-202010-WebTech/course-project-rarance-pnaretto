<%= form_with(model: comment_comment_report, local: true) do |form| %>
  <% if comment_comment_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_comment_report.errors.count, "error") %> prohibited this comment_comment_report from being saved:</h2>

      <ul>
        <% comment_comment_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
