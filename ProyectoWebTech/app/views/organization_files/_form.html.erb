<%= form_with(model: organization_file, local: true) do |form| %>
  <% if organization_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_file.errors.count, "error") %> prohibited this organization_file from being saved:</h2>

      <ul>
        <% organization_file.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
