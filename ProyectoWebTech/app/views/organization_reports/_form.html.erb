<%= form_with(model: organization_report, local: true) do |form| %>
  <% if organization_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_report.errors.count, "error") %> prohibited this organization_report from being saved:</h2>

      <ul>
        <% organization_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
