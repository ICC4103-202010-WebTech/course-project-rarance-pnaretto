<%= form_with(model: event_date_vote, local: true) do |form| %>
  <% if event_date_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_date_vote.errors.count, "error") %> prohibited this event_date_vote from being saved:</h2>

      <ul>
        <% event_date_vote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
