<%= form_with(model: date_notification, local: true) do |form| %>
  <% if date_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(date_notification.errors.count, "error") %> prohibited this date_notification from being saved:</h2>

      <ul>
        <% date_notification.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
