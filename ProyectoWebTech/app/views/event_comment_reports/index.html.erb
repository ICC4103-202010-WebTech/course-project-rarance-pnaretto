<p id="notice"><%= notice %></p>

<h1>Event Comment Reports</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_comment_reports.each do |event_comment_report| %>
      <tr>
        <td><%= link_to 'Show', event_comment_report %></td>
        <td><%= link_to 'Edit', edit_event_comment_report_path(event_comment_report) %></td>
        <td><%= link_to 'Destroy', event_comment_report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event Comment Report', new_event_comment_report_path %>
