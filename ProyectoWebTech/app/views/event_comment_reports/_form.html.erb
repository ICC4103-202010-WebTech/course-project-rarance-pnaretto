<%= form_with(model: event_comment_report, local: true) do |form| %>
  <% if event_comment_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_comment_report.errors.count, "error") %> prohibited this event_comment_report from being saved:</h2>

      <ul>
        <% event_comment_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
