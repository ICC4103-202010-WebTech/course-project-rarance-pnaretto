<%= form_with(model: organization_file_report, local: true) do |form| %>
  <% if organization_file_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_file_report.errors.count, "error") %> prohibited this organization_file_report from being saved:</h2>

      <ul>
        <% organization_file_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
